# Minimal Node API

A Node.js Express application with a single health endpoint.

## Tech Stack

- Node.js 22 with Express
- No database, no authentication

## Features

- `GET /health` returns `{ "status": "healthy" }` with a 200 status code
